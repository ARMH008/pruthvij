/* eslint-disable react/prop-types */
// Layout.js

// Layout.js
import React from "react";
//import { Outlet } from "react-router-dom"; // Use Outlet instead of Route
import Paperbase from "./Paperbase";

const Layout = () => (
  <React.Fragment>
    <Paperbase />
  </React.Fragment>
);

export default Layout;
